{% set nginx = salt['grains.filter_by']({
    'Debian': {
      'loadbalancers': {
        'redirect_to_www': False,
        'front': {
          'https_port': None,
          'servers': [],
        }
      },
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('nginx',{})) %}

