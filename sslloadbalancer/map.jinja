{% set nginx = salt['grains.filter_by']({
    'Debian': {
      'loadbalancers': {
        'front': {
          'sticky': True,
          'https_port': None,
          'servers': [],
        }
      },
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('nginx',{})) %}

