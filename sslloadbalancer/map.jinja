{% set nginx = salt['grains.filter_by']({
    'Debian': {
      'loadbalancers': {
        'canonical_host': False,
        'front': {
          'https_port': None,
          'servers': [],
        }
      },
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('nginx',{})) %}

